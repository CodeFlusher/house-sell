
export type MainPageCard = {
    name: string,
    description: string,
    icon: Component,
}

export type MobileNavButton = {
    id: string,
    icon: Component,
}

export type OrderPhase = {
    id: string,
    use: boolean,
}

export type DesignerCard = {
    name: string,
    image: string,
    description: string,
    rating: number,
}